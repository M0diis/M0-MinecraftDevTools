# Example Scripts

These sample scripts are grouped by use case.

Available bindings in the script context:
- `client`
- `source`
- `player`
- `world`
- `options`
- `server`

Each example is mirrored in Groovy (`.groovy`), Kotlin (`.kts`), and JavaScript (`.js`).
